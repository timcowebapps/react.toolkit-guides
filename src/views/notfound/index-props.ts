'use strict';

import * as PropTypes from 'prop-types';

export namespace NotFoundPageProps {
	export interface IProps {
		// Empty
	}

	export const types: PropTypes.ValidationMap<IProps> = {
		// Empty
	}

	export const defaults: IProps = {
		// Empty
	}
}
